# MongoDB connection string (get from MongoDB Atlas or local)
MONGODB_URI=mongodb+srv://<user>:<password>@cluster.mongodb.net/quico?retryWrites=true&w=majority

# JWT secret — use a long random string (e.g. openssl rand -base64 32)
JWT_SECRET=your_super_secret_jwt_key_here_make_it_long_and_random

# Groq API key — get from https://console.groq.com/keys
GROQ_API_KEY=gsk_...

# Next.js public URL (used for production)
NEXTAUTH_URL=http://localhost:3000

# --- Optional: enables real web search results for Notes Finder & PYQ ---
# Get free key at https://serpapi.com (100 searches/month free)
SERPAPI_KEY=

# --- Optional: enables real YouTube video metadata ---
# Create key at https://console.cloud.google.com → YouTube Data API v3
YOUTUBE_API_KEY=
